import { Meta, Controls, Story, Canvas, ArgTypes, Title } from '@storybook/blocks';
import * as Button from './button.stories';

<Meta title="Atom/Button" name="Documentation" component={Button} />

<Title>Button</Title>

Buttons allow users to take actions, and make choices with a single tap.

### Usage

- A button should contain at least one text.
- The first letter of the word on the buttons is capitalized and the rest is lowercase.
- Button texts are limited to one line.

## Button variants

We have only 1 variant for this button: Default

<Canvas of={Button.Default} />

## Using button with Drupal

```twig
{% include '@storybook_example/button/button.twig' with { content: 'Submit'} %}
```

## References

<ArgTypes of={Button} />
