services:
  stream.twig:
    class: Drupal\stream\StreamTwigExtension
    tags:
      - { name: twig.extension }

  stream_wrapper.theme:
    class: Drupal\stream\StreamWrapper\ThemeStream
    tags:
      - { name: stream_wrapper, scheme: theme }